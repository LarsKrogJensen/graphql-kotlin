machine:
  java:
    version: oraclejdk8
dependencies:
  pre:
    - curl -fL https://getcli.jfrog.io | shtest:
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/graphql-kotlin
    - mv target/site  $CIRCLE_ARTIFACTS/graphql-kotlin/coverage-report
    - bash <(curl -s https://codecov.io/bash)